# Git 自动同步配置说明

## 功能说明

本项目已配置 Git 自动同步功能，每次执行 `git commit` 后会自动推送到 GitHub。

## 工作原理

在 `.git/hooks/post-commit` 文件中配置了提交后钩子（post-commit hook），每次提交后自动执行：
```bash
git push origin main
```

## 使用示例

```bash
# 修改代码后，只需要执行提交，推送会自动完成
git add .
git commit -m "你的提交信息"

# 提交后会自动显示：
# 🚀 正在自动推送到 GitHub...
# ✅ 推送成功！
```

## 优点

- **简化操作**：无需手动执行 `git push`
- **防止遗忘**：自动推送，避免本地提交未同步
- **提高效率**：减少重复操作

## 禁用自动同步（可选）

如果不需要自动推送，可以禁用钩子：

```bash
# 方式 1：删除钩子文件
rm .git/hooks/post-commit

# 方式 2：禁用钩子（保留文件）
chmod -x .git/hooks/post-commit

# 方式 3：临时跳过（单次）
git commit --no-verify -m "提交信息"
```

## 重新启用自动同步

如果之前禁用了，可以重新启用：

```bash
# 设置可执行权限
chmod +x .git/hooks/post-commit

# 或者重新创建钩子
cat > .git/hooks/post-commit << 'EOF'
#!/bin/bash
echo "🚀 正在自动推送到 GitHub..."
git push origin main
if [ $? -eq 0 ]; then
    echo "✅ 推送成功！"
else
    echo "❌ 推送失败，请检查网络连接或权限"
fi
EOF
chmod +x .git/hooks/post-commit
```

## 注意事项

1. **网络要求**：需要能正常访问 GitHub
2. **权限要求**：需要有仓库的推送权限
3. **推送失败**：如果推送失败，会显示错误信息，需要手动处理
4. **多分支**：当前配置只推送到 main 分支，如需其他分支需要修改钩子

## 故障排查

### 推送失败

如果看到推送失败，检查：
- 网络连接是否正常
- 是否有推送权限
- 是否有合并冲突

```bash
# 手动推送查看详细错误信息
git push origin main -v
```

### 钩子未执行

如果提交后没有看到推送消息：
```bash
# 检查钩子权限
ls -la .git/hooks/post-commit

# 应该显示：-rwxr-xr-x ... .git/hooks/post-commit
```
